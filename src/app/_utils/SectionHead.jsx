import React from 'react';

const SectionHead = ({ title }) => {
    return (
        <>
            <h2 className="text-primary font-bold text-5xl text-center mt-24 mb-12">{title}</h2>

        </>
    );
};

export default SectionHead;